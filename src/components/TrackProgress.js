import React from 'react';
import '../App.css';

const TrackProgress = () => {
  return (
    <div className="track-progress">
      <h1>Track Progress</h1>
      {/* Add the functionality to track user progress here */}
    </div>
  );
};

export default TrackProgress;